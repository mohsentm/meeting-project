language: php

php:
  - 7.1

dist: trusty

services:
  - postgresql

before_script:
  - psql -c 'create database meetingdb;' -U postgres
  - copy .env.config .env
  - composer install
  - php artisan key:generate
  - artisan migrate

script:
  - vendor/bin/phpunit
